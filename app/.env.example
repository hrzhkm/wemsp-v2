# Database URL for PostgreSQL
# For Docker: use host.docker.internal instead of localhost
DATABASE_URL=

# Better auth
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000 # Base URL of your app

# Comma-separated emails promoted to ADMIN role by `pnpm db:seed`.
# Users must sign up through the normal login first; seeding only elevates existing accounts.
ADMIN_EMAILS=admin@wemsp.com

# Bootstrap admin account created by `pnpm db:seed` (via better-auth signup).
# The account is created with a usable password, marked email-verified, and promoted to ADMIN.
# Set a strong ADMIN_PASSWORD before seeding anything beyond local dev.
ADMIN_EMAIL=admin@wemsp.com
ADMIN_PASSWORD=
ADMIN_NAME=System Admin

# API Base URL (for client-side requests)
# For Docker: use the exposed port (e.g., http://localhost:5050)
VITE_API_BASE_URL=http://localhost:3000

# Admin app origin for CORS
ADMIN_ORIGIN=http://localhost:3001

# Google OAuth - https://www.better-auth.com/docs/authentication/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_APP_PASSWORD=
EMAIL_ADMIN=

# AWS
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET=

# OpenRouter (LangChain ChatOpenAI-compatible endpoint)
OPENROUTER_API_KEY=
OPENROUTER_MODEL=moonshotai/kimi-k2.5
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# Optional ranking headers for OpenRouter
OPENROUTER_SITE_URL=
OPENROUTER_APP_NAME=WEMSP

# Contract
PRIVATE_KEY=
RPC_URL=
CONTRACT_ADDRESS=